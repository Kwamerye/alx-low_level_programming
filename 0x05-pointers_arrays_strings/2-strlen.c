#include "main.h"

/**
 * _strlen - a funtion that returns the length of a string
 * @s: char input
 * Return: length as integer;
 */

int _strlen(char *s)
{
	int i = 1, sum = 0;
	char pl = s[0];

	while (p1 != '\0')
	{
		sum++;
		pl = s[i++];
	}
	return (sum);
}
