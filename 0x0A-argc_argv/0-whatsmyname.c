#include <stdio.h>

/**
 * main - prints the program name, followed by a new line.
 * @argc: The number the arguement supplied to the program.
 * @argv: An array of thepointer to the arguement.
 *
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
